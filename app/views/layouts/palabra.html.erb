<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:400,400italic' rel='stylesheet' type='text/css' />

    <title><%= (@words.first.name + " - " rescue "") + t('inline_forms.general.application_title') %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <% if thumbnail = @words.first.pictures.first %>
        <!--
          <PageMap>
            <DataObject type="thumbnail">
              <Attribute name="src" value="https://bankodipalabra.com<%= thumbnail.image.palm.url %>"/>
              <Attribute name="width" value="250"/>
              <Attribute name="height" value="250"/>
            </DataObject>
          </PageMap>
        -->
    <% end %>

    <%= csrf_meta_tags %>
  </head>

  <body>
    <div id="frontend">
      <% @logo_right = 'papiamentu-home.png' %>
      <%= render "/shared/frontend_header" %>
      <div class="row">
        <%= render "shared/flash" %>
        <%= yield %>
      </div>
    </div>

    <%= render "/shared/frontend_footer" %>
    <%= javascript_include_tag 'application' %>

  </body>
</html>
